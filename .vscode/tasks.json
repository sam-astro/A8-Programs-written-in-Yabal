{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Yabal Program",
            "type": "shell",
            "command": "./yabal.exe build ${file}; astro8 ${fileDirname}/${fileBasenameNoExtension}.asm;",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
  }